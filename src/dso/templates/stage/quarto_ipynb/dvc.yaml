stages:
  "{{ name }}":
    params:
      - dso.quarto
    deps:
      - src/{{ name }}.ipynb
    outs:
      - output
      - report/{{ name }}.html
    cmd:
      - mkdir -p output
      - uv run dso exec quarto .

stages:
  "{{ name }}":
    params:
      - dso.quarto
    deps:
      - src/{{ name }}.qmd
    outs:
      - output
      - report/{{ name }}.html
    cmd:
      - mkdir -p output
      - dso exec quarto .
